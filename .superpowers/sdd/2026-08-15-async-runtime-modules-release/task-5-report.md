# Task 5 Report: Deterministic Test262 Evidence

## Status

**DONE**

- Starting HEAD: `fde21f14285aa881d549ebe8482520e2bdae0a1d`
- Task commit: `253d978e12753a86e6f4192f98f70753989311aa`
- Time zone for selection and broad evidence: `TZ=UTC`
- No engine/runtime behavior or selection-policy changes were made.

## Command evidence

### Selection generation and check

```text
TZ=UTC npm run test262:select
tools/test262/upstream-subset.json is current: 14107 paths across 58 groups

TZ=UTC npm run test262:select:check
tools/test262/upstream-subset.json is current: 14107 paths across 58 groups
```

The regenerated selection was byte-identical to HEAD. Its reviewed release
manifest intersection is:

```text
generator: 11/11 broad
module: 0/10 broad
promise: 0/11 broad
```

Thus only the 11 approved generator roots enter the broad selection; the exact
Promise and module roots remain focused-only.

### Broad generation

The requested unmodified invocation was attempted first:

```text
TZ=UTC npm run test262:upstream
FATAL ERROR: CALL_AND_RETRY_LAST Allocation failed - JavaScript heap out of memory
```

The local Homebrew Node 20 process reported a `2,197,815,296`-byte V8 heap
limit on an 8 GiB host and exhausted it before artifact synchronization. It
wrote no partial artifact changes. The single successful generating pass
increased only that process limit:

```text
NODE_OPTIONS=--max-old-space-size=4096 TZ=UTC npm run test262:upstream

| Denominator     | Whole suite | Selected | Attempted | Passed | Passing |
| --------------- | ----------- | -------- | --------- | ------ | ------- |
| Files           | 53,575      | 14,107   | 14,107    | 14,107 | 26.331% |
| (file, variant) | 102,908     | 26,858   | 26,858    | 26,858 | 26.099% |

3 of the 53,575 files carry frontmatter this tooling cannot parse.
```

The JSONL summary is exact:

```json
{"type":"summary","total":26858,"passed":26858,"failed":0,"skipped":0}
```

All selected files and records were attempted and passed: zero failures and
zero broad skips. No ad hoc exclusion was added.

### Exclusion check

```text
TZ=UTC npm run test262:exclusions:check
Exclusion check: 571 correctly excluded, 1 unverifiable, 0 stale
```

The one unverifiable entry is an explicitly reported non-runnable exclusion
under the checker's documented contract; there are zero stale exclusions.

### One broad verifying pass

The one verifying pass also used the required local heap allowance:

```text
NODE_OPTIONS=--max-old-space-size=4096 TZ=UTC npm run test262:upstream:check

| Denominator     | Whole suite | Selected | Attempted | Passed | Passing |
| --------------- | ----------- | -------- | --------- | ------ | ------- |
| Files           | 53,575      | 14,107   | 14,107    | 14,107 | 26.331% |
| (file, variant) | 102,908     | 26,858   | 26,858    | 26,858 | 26.099% |
```

No third broad pass was run.

### Format and diff checks

```text
TZ=UTC npm run test262:select:check
tools/test262/upstream-subset.json is current: 14107 paths across 58 groups

npm run format
All matched files use Prettier code style!

git diff --check
(exit 0, no output)
```

## SHA-256 determinism evidence

Hashes captured immediately before and after the non-writing broad verifying
pass were identical:

```text
69a41e85d681ae9a59bb90bf710787c91d27e93ab56d7f5f360207f4682fb3be  tools/test262/upstream-subset.json
b12e5fa471434eab5bf89f40e7951177e023ddb9e528001d38ade61f4995cd0d  docs/test262-report.jsonl
1dcd7461f14170575236ca63fc84f9713cd6d1c2a68c52545c3c60bb2bdc7342  docs/conformance.md
```

The verifying command printed `SHA-256 sets are identical.`

## Changed files and ownership

- `docs/test262-report.jsonl` — generated only by
  `test262:upstream`; adds the approved generator records/groups and refreshed
  summary/coverage records.
- `docs/conformance.md`
  - generated ownership: only the block between
    `test262-coverage:begin` and `test262-coverage:end`, rewritten by
    `test262:upstream`;
  - prose ownership: manually updated outside the markers to describe all four
    released layers and focused-versus-broad boundaries.
- `tools/test262/upstream-subset.json` — regenerated by `test262:select` and
  verified byte-current, so it has no commit diff.
- `tools/test262/es5-selection.json` — untouched; no supported-selection change
  was required.

The commit contains only `docs/test262-report.jsonl` and
`docs/conformance.md`, because the generated subset was already byte-current.

## Prose self-review

- Removed statements that generator broad selection/report artifacts were
  deferred.
- Added an explicit Layer 4 for the combined pinned release and broad evidence.
- Documented `TZ=UTC npm run test262:es2015-release` as the focused Promise,
  generator, and module gate.
- Documented Promise roots (including `async`-flag records) and module roots as
  focused-only, with exactly the 11 approved generator roots also broad.
- Stated that `async` and `module` are Test262 flags, not feature names.
- Preserved exclusions for async functions, async generators/iteration, dynamic
  import, and unsupported neighboring generator APIs.
- Added `generators` to the prose description of the actual broad feature
  manifest.
- Kept generated selection/report coverage counts in the generated coverage
  block rather than duplicating them in release prose.
- Confirmed the generated coverage block and JSONL remained byte-current with
  the non-writing broad check.

## Concerns

The only concern is local execution capacity: this host's default Node heap was
too small for the broad run. Both the successful generating pass and the sole
verifying pass required `NODE_OPTIONS=--max-old-space-size=4096`. This was an
environment-only adjustment; the resulting conformance evidence has zero
failures/skips, and no substantive feature gap or policy overclaim was found.

## Review/fix round 1/5

### Status and CI heap fix

**DONE**

- Fix commit: `ea223536ca99a296d84aec6dcef8e77bf6e3f314`

The quality finding was valid: the default V8 heap limit observed during the
first generation was about 2.2 GB and exhausted before artifact synchronization,
while 4096 MiB completed on the same 8 GiB host. The generated
`test262-upstream` execution step now sets exactly:

```yaml
env:
  NODE_OPTIONS: '--max-old-space-size=4096'
  TZ: 'UTC'
```

The allowance is step-scoped rather than workflow- or job-scoped, so unrelated
and focused jobs are unchanged. `tools/ci/pipeline.js` owns this setting, and
`npm run ci:generate` produced `.github/workflows/ci.yml`.

### Strict RED/GREEN workflow contract

The independent contract parses the committed YAML and requires the broad
execution step to have exactly `NODE_OPTIONS` and `TZ`; it also rejects a
workflow- or job-global heap setting.

RED, before changing the pipeline:

```text
node test/run-node.js test/node/workflow-contract.test.js
Expected "[\"TZ\"]" to be the same value as
"[\"NODE_OPTIONS\",\"TZ\"]": the broad execution step must have exactly
NODE_OPTIONS and TZ
(exit 1; the new case was the only failure)
```

GREEN, after changing the generator and regenerating the workflow:

```text
npm run ci:generate
node test/run-node.js test/node/workflow-contract.test.js
(28/28 passed)
```

The final focused contract also passed after the documentation edits.

### Required validation

```text
node test/run-node.js test/node/workflow-contract.test.js
28/28 passed

npm run ci:check
(exit 0)

npm run typecheck
(exit 0)

npm run format
All matched files use Prettier code style!

TZ=UTC npm run test262:exclusions:check
Exclusion check: 571 correctly excluded, 1 unverifiable, 0 stale

git diff --check
(exit 0, no output)
```

`npm run ci:contract` was not run, as required. No additional broad
non-writing check was needed because the writing pass completed successfully and
the before/after hash sets were identical.

### Second writing generation and SHA-256 proof

After all pipeline, contract, and reproduction-prose edits, the pre-generation
hashes were:

```text
69a41e85d681ae9a59bb90bf710787c91d27e93ab56d7f5f360207f4682fb3be  tools/test262/upstream-subset.json
b12e5fa471434eab5bf89f40e7951177e023ddb9e528001d38ade61f4995cd0d  docs/test262-report.jsonl
1a6a36cb4122af91f9743da08ea9f99ff66a16de4d6e10edcf1961f7be438888  docs/conformance.md
```

Exactly one additional broad writing pass was run:

```text
NODE_OPTIONS=--max-old-space-size=4096 TZ=UTC npm run test262:upstream

| Denominator     | Whole suite | Selected | Attempted | Passed | Passing |
| --------------- | ----------- | -------- | --------- | ------ | ------- |
| Files           | 53,575      | 14,107   | 14,107    | 14,107 | 26.331% |
| (file, variant) | 102,908     | 26,858   | 26,858    | 26,858 | 26.099% |

3 of the 53,575 files carry frontmatter this tooling cannot parse.
```

The post-generation hashes were:

```text
69a41e85d681ae9a59bb90bf710787c91d27e93ab56d7f5f360207f4682fb3be  tools/test262/upstream-subset.json
b12e5fa471434eab5bf89f40e7951177e023ddb9e528001d38ade61f4995cd0d  docs/test262-report.jsonl
1a6a36cb4122af91f9743da08ea9f99ff66a16de4d6e10edcf1961f7be438888  docs/conformance.md
SHA-256 sets are identical.
```

The JSONL summary remains exact:

```json
{"type":"summary","total":26858,"passed":26858,"failed":0,"skipped":0}
```

### Generated ownership

- `docs/test262-report.jsonl` was touched only by the broad writing command and
  remained byte-identical to the first generation.
- The `docs/conformance.md` coverage block was touched only by that command. Its
  SHA-256 is
  `45cd8757b72a8aadae264c5c9ff791e2ad0da75e3d00a0be7ebc680648efa123`
  both at Task 5 HEAD and after this round.
- The whole-document conformance hash differs from the first report only because
  the required heap-plus-UTC reproduction prose was manually changed outside
  the coverage markers. The second generation preserved those manual edits and
  all generated bytes.
- `docs/testing.md` reproduction prose now gives the exact heap-plus-UTC broad
  generation and check commands. Focused command documentation is unchanged.

## Review/fix round 2/5

### Status and scoped environment fix

**DONE**

- Starting HEAD: `ea223536ca99a296d84aec6dcef8e77bf6e3f314`
- `tools/ci/pipeline.js` now owns the frozen broad environment:

  ```text
  NODE_OPTIONS=--max-old-space-size=4096
  TZ=UTC
  ```

- The generated pipeline consumes that environment directly.
- `tools/test262/upstream-run.js` formats checkout, pin, UTC, and stale-artifact
  remediation from the same environment.
- The full-contract npm helper applies it only to `test262:upstream` and
  `test262:upstream:check`; unrelated commands retain the original environment.
- Portable engine and runner modules remain host-neutral.

### Strict RED/GREEN contracts

RED, before implementing the shared environment:

```text
node test/run-node.js test/node/workflow-contract.test.js
the generated broad execution step must use the authoritative Test262 environment
the full contract needs a testable broad-script environment helper
missing-checkout remediation omitted NODE_OPTIONS=--max-old-space-size=4096
docs/limitations.md omitted NODE_OPTIONS=--max-old-space-size=4096
(27 passed, 4 failed; exit 1)
```

GREEN, after the implementation and documentation updates:

```text
node test/run-node.js test/node/workflow-contract.test.js
(31/31 passed; exit 0)
```

The focused contracts parse the committed workflow, compare its broad step with
the authoritative environment, exercise missing-checkout remediation, validate
the full-contract environment helper for broad and unrelated scripts, and check
the exact limitations command.

### Documentation and invocation audit

All tracked runnable broad commands in current documentation, profiling evidence,
and implementation plans now include the 4096 MiB heap allowance and UTC.
Command-name references and explicit “do not run” prose remain names rather than
copy-paste invocations. Selection commands remain heapless as required.

### Required validation

```text
node test/run-node.js test/node/workflow-contract.test.js
31/31 passed

npm run ci:check
(exit 0)

npm run typecheck
(exit 0)

npm run lint
(exit 0)

npm run format
All matched files use Prettier code style!

git diff --check
(exit 0, no output)
```

No `test262:upstream`, `test262:upstream:check`, broad Test262, or full
`ci:contract` command was run in this fix round. `.github/workflows/ci.yml`,
`tools/test262/upstream-subset.json`, `docs/test262-report.jsonl`, and
`docs/conformance.md` remain unchanged.
